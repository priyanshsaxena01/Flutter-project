package com.example.fraud_shield

import io.flutter.embedding.android.FlutterFragmentActivity

// local_auth shows the system fingerprint dialog, which needs a
// FragmentActivity.
class MainActivity : FlutterFragmentActivity()
